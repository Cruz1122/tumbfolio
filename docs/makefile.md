# Makefile

Comandos principales:

```bash
make install
make dev
make dev-web
make dev-api
make dev-worker
make typecheck
make lint
make test
make docker-up
make docker-down
```

`make dev` levanta tres procesos: Next.js, NestJS API y NestJS worker.
